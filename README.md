# MafAgent

MafAgent is a .NET 9 solution that provides a console application host and a reusable Core library for building a Microsoft Agent Framework agent in a future iteration, with an xUnit test project for validating the shared application logic.